subprojects {
    version = '0.1.0'
    group = 'ch.fhnw.swa.modserv'

    repositories {
        jcenter()
    }

    plugins.withType(JavaPlugin).configureEach {
        java {
            modularity.inferModulePath = true
        }
    }
}
